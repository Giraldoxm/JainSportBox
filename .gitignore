# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.db

# Variables de entorno — nunca subir al repo
.env

# Uploads (fotos de usuarios)
backend/uploads/

# Node
frontend/node_modules/
frontend/dist/
frontend/dev-dist/

# Variables de entorno del frontend (valores por máquina/entorno)
frontend/.env.local
frontend/.env.production

# Build Release del bridge (el Debug SÍ va trackeado: la PC del gym corre
# bin/Debug/net48 sin compilar)
servicio_biometrico/bin/Release/
servicio_biometrico/obj/Release/

# Logs en runtime (el bridge mantiene bridge.log abierto y bloqueaba los cambios de rama)
*.log

# Claude
.claude/
